services:
  multipanel:
    container_name: multipanel
    image: multipanel:prod
    build:
      context: .
    restart: unless-stopped
    ports:
      - "8082:8082"
    volumes:
      - ./config.toml:/app/config.toml:ro
